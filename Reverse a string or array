Reverse a string or array(https://practice.geeksforgeeks.org/problems/reverse-a-string/1)


string reverseWord(string str){
    int size = str.length();
    int l = 0;
    int r = size-1;
    while(l<r){
        char swap = str[l];
        str[l] = str[r];
        str[r] = swap;
        l++;
        r--;
    }
    return str;
}
